@model IEnumerable<GameZone.Models.FaixaEtaria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoDeClassificacao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoDeClassificacao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FaixaEtariaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.FaixaEtariaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FaixaEtariaId })
        </td>
    </tr>
}

</table>
